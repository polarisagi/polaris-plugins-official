[project]
name = "system-info"
version = "0.1.0"
description = "Polaris MCP plugin: Provides OS environment context (system info, locale, timezone, installed apps) to the AI Agent."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
