[project]
name = "knowledge-base"
version = "0.1.0"
description = "Polaris MCP plugin: Provides safe, sandboxed access to local files and directories for RAG purposes."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
