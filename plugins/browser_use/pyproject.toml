[project]
name = "browser-use"
version = "0.1.0"
description = "Polaris MCP plugin: Browser automation via DOM extraction and Playwright for precise, ID-based web element interaction."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp>=1.27.2",
    "playwright>=1.60.0",
]
